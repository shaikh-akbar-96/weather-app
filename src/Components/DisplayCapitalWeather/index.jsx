import React from "react";

const DisplayCapitalWeather = ({ capital, lat, lag }) => {
  // func ()=> return weather data;

  return <div>DisplayCapitalWeather</div>; // display capital weather
};

export default DisplayCapitalWeather;
